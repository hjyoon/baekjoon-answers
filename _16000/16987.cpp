#include <iostream>
#include <vector>

using namespace std;

int atk[8];
int hp[8];
bool die[8];

int main() {
	cin.tie(NULL);
    ios_base::sync_with_stdio(0);
    freopen("input.txt", "r", stdin);

    int n = 0;
    cin >> n;

    for(int i=0; i<n; i++) {
        cin >> hp[i] >> atk[i];
    }

    while(1) {
        
    }



	return 0;
}